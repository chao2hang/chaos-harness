/** Host loader entry for the System maintenance section exported from `./client`. */

/** Host plugin body — the maintenance section is browser-only; the Host half of restarting is `host.restart`. */
export function apply(): void {}
